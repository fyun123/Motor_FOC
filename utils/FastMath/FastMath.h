#ifndef __FASTMATH_H
#define __FASTMATH_H

#include "LUT.h"
#include "math_ops.h"
namespace FastMath{
    float FastSin(float theta);
    float FastCos(float theta);
		float FastAtan(float tan);
    };

#endif
